Create a MVC Application with following features.
1.Create Model Customer(CustomerId,CustomerName,Email,Mobile,Address)
2.Create Customer Repository with
	GetAllCustomers()
	GetCustomer()
	AddCustomer()
3.Create CustomerController and define the action methods to invoke repo functions.
4.Create views and bind thhe models as the the requirement.